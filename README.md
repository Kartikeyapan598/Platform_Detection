# Platform_Detection

Compile Time Platform detections and utilities-

	*`Compiler.h`
	Compiler Detection Macros
	-macros:
	 *`COMPILER_CLANG`
	 *`COMPILER_INTEL`
	 *`COMPILER_MSVC`
	 *`COMPILER_GNUC`

	*`Environment.h`
	-macros:
	 *`ENVMT_MINGW`
	 *`ENVMT_CYGWIN`
	 *`ENVMT_UNIX`